<section class="posts">
  {{#each posts as |post|}}
  <div class="card">
    <h3 class="card-header"><a href="/post/{{id}}"> {{title}}</a></h3>
    <p>Posted by {{user.username}} on {{format_date date_created}} </p>
  </div>
  <p class="card-body">{{content}}</p>
  </div>
  {{/each}}
</section>